/* 
 * Licenca GPLv3
 */
define([
    'radio',
    'i18next',
    'app/bars',
    'backbone',
    'marionette',
    'app/filter/View/DualListView'
], function (
        Radio,
        i18next,
        Handlebars,
        Backbone,
        Marionette,
        DualListView
        ) {

    var IzbiraCasView = DualListView.extend({});

    return IzbiraCasView;
});