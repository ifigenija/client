/* 
 * Licenca GPLv3
 * 
 * testi:
 *      vstavi select collection
 *      pridobi selected collection
 */

define([
    'radio',
    'i18next',
    'app/bars',
    'backbone',
    'underscore',
    'marionette'
], function (
        Radio,
        i18next,
        Handlebars,
        Backbone,
        _,
        Marionette
        ) {

    describe("DualListCollView", function () {
        beforeEach(function () {
        });
        afterEach(function () {
        });
        it('test', function () {
        });
    });
});